{
  "name": "tiresias",
  "version": "0.1.0",
  "description": "Tiresias - AI 에이전트 기반 집단 지능 예측 엔진",
  "scripts": {
    "setup": "npm install && cd frontend && npm install && cd ../workers && npm install",
    "setup:backend": "cd backend && uv sync",
    "setup:all": "npm run setup && npm run setup:backend",
    "dev": "concurrently --kill-others -n \"backend,frontend\" -c \"green,cyan\" \"npm run backend\" \"npm run frontend\"",
    "dev:workers": "cd workers && npx wrangler dev",
    "backend": "cd backend && uv run python run.py",
    "frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "deploy": "npm run build && cd workers && npx wrangler deploy",
    "db:init": "cd workers && npx wrangler d1 execute tiresias-db --file=schema.sql",
    "db:init:local": "cd workers && npx wrangler d1 execute tiresias-db --local --file=schema.sql",
    "db:migrate": "cd workers && npx wrangler d1 migrations apply tiresias-db",
    "db:migrate:local": "cd workers && npx wrangler d1 migrations apply tiresias-db --local",
    "ads:check": "node scripts/google-ads/validate-config.mjs",
    "ads:plan": "node scripts/google-ads/generate-plan.mjs",
    "ads:campaign:sync": "node scripts/google-ads/sync-search-campaign.mjs",
    "ads:conversions:create": "node scripts/google-ads/create-conversion-actions.mjs",
    "ads:report:search-terms": "node scripts/google-ads/fetch-search-terms.mjs",
    "ads:conversions:upload": "node scripts/google-ads/upload-offline-conversions.mjs",
    "naver:ads:check": "node scripts/naver-ads/validate-config.mjs",
    "naver:ads:plan": "node scripts/naver-ads/generate-plan.mjs",
    "naver:ads:campaigns": "node scripts/naver-ads/list-campaigns.mjs",
    "naver:ads:estimate": "node scripts/naver-ads/estimate-keywords.mjs",
    "naver:ads:campaign:sync": "node scripts/naver-ads/sync-search-campaign.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "AGPL-3.0",
  "dependencies": {
    "playwright": "^1.58.2"
  }
}
